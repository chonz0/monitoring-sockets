Monitoring mocked data WebSockets server
===========================================

## Getting Started

1. Clone this repo
2. Run `yarn` to install dependencies
3. Start with `node index` or `nodemon index`